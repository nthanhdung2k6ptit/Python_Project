git pull origin main     # Lấy code mới nhất từ GitHub về máy
git add .                # Thêm file bạn vừa sửa
git commit -m "Cập nhật phần API của Thành viên 1"
git push origin main     # Đẩy code lên GitHub
Nghĩa là:

pull trước để không đè code người khác

push sau khi commit xong